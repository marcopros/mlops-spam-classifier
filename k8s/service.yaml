apiVersion: v1
kind: Service
metadata:
  name: spam-classifier
  labels:
    app: spam-classifier
spec:
  type: ClusterIP
  selector:
    app: spam-classifier
  ports:
    - port: 80
      targetPort: 10000
      protocol: TCP
